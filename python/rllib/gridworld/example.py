import numpy as np

LF_MIN = 1
RT_MAX = 10

init_positions = list(range(LF_MIN,RT_MAX))
A = "{----------}"

lst = []
'''
for i in len(A) :
    lst.append(i)


'''
position = np.random.choice(init_positions)
print(init_positions)
print(position)
